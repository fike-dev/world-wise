# Front-end App (React + Vite App)

## Feature

### Keeps track of places you have traveled 📍

### Has fake authentication (login and logout) 🔒

### Uses fake database (json data) in the project folder

## Technologies used

- React + Vite
- Css modules for styling
- react-router-dom for routing
- leaflet to interact with the map

## How to run this app

- clone this project `git clone https://github.com/fike-dev/world-wise.git` in to your local repository.

- `npm i` to install node modules.

- `npm run server` to initialize the data.json file that uses as a data server
- `npm run dev` to run the actual app

### Got any issues running this project? contact me `fikadumom@gmail.com`
